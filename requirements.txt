# Production server dependencies (client excluded)
fastmcp==2.11.3
# Minimal runtime needs only fastmcp; below are optional if server evolves
# mcp==1.13.1
# langchain==0.3.27
# langchain-mcp-adapters==0.1.9
# langchain-openai==0.3.32
# langgraph==0.6.6
# python-dotenv==1.1.1
